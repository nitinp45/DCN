#include<stdio.h>

int main()
{
 int arr[6];
 char src[50],dst[50];
 int i, j, k,n,str;
 printf("Enter the size of array: ");
 scanf("%d",&n);


 printf("Enter the Element: ");
 for(i=0;i<50;i++)
 { 
  scanf("%s",&src[i]);
 }

 for(i=0;i<=0;i++)
 {
   if(src[i]==arr[j])
   {
    dst[k]==src[i];
    j++;
    k++;
   }
   else
   dst[k]==src[i];
 }
 printf("%c",dst[k]);
}
